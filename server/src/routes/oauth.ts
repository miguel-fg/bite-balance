import { Router } from "express";

const router = Router();

router.get("/google", () => {});
router.get("/google/callback", () => {});

router.get("/github", () => {});
router.get("/github/callback", () => {});

export default router;
